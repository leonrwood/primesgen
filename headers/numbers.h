#ifndef LEON_NUMBERS_H
#define LEON_NUMBERS_H

int digitsofnumber(unsigned long long int x);
bool isintoverflow(char* num, unsigned long long int max);
bool isinteger(char* str);
int isprime(unsigned long long x);

#endif
